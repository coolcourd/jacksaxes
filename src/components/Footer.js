import { Link } from "react-router-dom";

const Footer = () => {
    return (
        <footer>
            <h2> Copywrite {new Date().getFullYear()} </h2>
        </footer>
    )
}
export default Footer;